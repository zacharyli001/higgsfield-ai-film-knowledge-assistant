# 版本历史

本文件记录工作区中实际保存并可追溯的版本。每一项都对应仓库中的 Git 标签和 `releases/` 安装包；没有保存的版本号不会补写或虚构。

## v3.10.8

- 校验离屏音频通道的真实启动结果，避免失败时误报成功。
- 从 Chrome 工具栏启动字幕后，立即同步网页控制条的开关状态。
- 页面按钮明确提示需要通过顶部插件图标授予标签页录音权限。
- 更新工具栏说明文字，使其与字幕启动行为一致。

## v3.10.7

- Apple Speech 改为单队列顺序识别，避免多个本地请求互相堵塞。
- 保留识别后的 DeepSeek 短句并发翻译能力。
- 禁用内嵌播放器中的第二套录音器，消除重复识别和永久等待提示。
- 将本地识别超时作为可跳过的瞬时错误处理。

## v3.10.6

- Community、原创系列和学院统一使用标签页音频捕获与 WAV 识别路径。
- 修复 Community 旧 WebM 音频被 Apple Speech 当作 WAV 读取的问题。
- 页面刷新时清除旧扩展实例残留的界面和字幕层。
- 无语音等瞬时错误不再作为字幕长期显示。

## v3.10.5

- Apple Speech 调整为单通道使用方式。
- 对常见无语音、系统识别错误增加一次快速重试和静默跳过。
- 降低多个本地识别任务竞争造成的超时概率。

## v3.10.4

- 新增独立“字幕翻译：开/关”按钮和明确的开关状态。
- 统一控制条、字幕任务与界面状态。

## v3.10.3

- 字幕内容在顶层页面与播放器框架之间同步。
- 使用 Popover 顶层显示并监听全屏事件，修复进入全屏后字幕消失。

## v3.10.2

- 新增仅中文、仅英文、英文先行后替换中文三种字幕输出模式。
- 仅中文模式避免先闪现英文；仅英文模式完全跳过翻译模型。
- 丢弃与当前字幕模式不一致的迟到结果。

## v3.10.1

- 增加 2、3、4、6 档视频短句并发量。
- 默认使用并发量 4，并为 SiliconFlow 速率限制提供降档提示。
- 并发设置只作用于字幕短句，不影响网页翻译和项目分析。

## v3.10.0

- 标签页音频转为 16 kHz 单声道 WAV。
- 将句末停顿降低到约 0.32 秒、最长分段降到约 4 秒，进入低延迟模式。
- 为字幕增加序号，自动丢弃迟到的旧结果。
- 最初引入两路识别并发，随后在 v3.10.5–v3.10.7 调整为更稳定的 Apple 单队列方案。

## v3.9.2

- 修复 Apple Speech Native Messaging 助手的事件循环。
- 增加本地识别超时控制、授权窗口和更明确的权限提示。
- 修复本地语音识别任务回调无法正常完成的问题。

## v3.9.0

- 首次接入 macOS Apple Speech 本地英文识别。
- 新增 Native Messaging 助手、安装脚本和系统权限检测。
- 可在 Apple 本地识别与在线语音接口之间切换。
- 字幕翻译可沿用当前模型接口或单独使用 DeepL。

## v3.8.1

- 修复音频 Data URL 与 MIME 类型解析。
- 改善在线语音接口上传音频时的格式兼容性。

## v3.8.0

- 引入 Chrome `tabCapture` 与 Offscreen Document。
- 支持学院内部封装播放器的标签页音频捕获。
- 增加工具栏启动字幕流程和离屏音频分段队列。

## v3.7.0

- 增加无字幕视频缓冲延迟设置。
- 使用隐藏的提前播放副本预取字幕，再延后正式视频播放。
- 接口暂时跟不上时可短暂停顿视频等待字幕。

## v3.6.0

- 增加语音活动检测，按句末停顿切分无字幕视频。
- 约 0.65 秒静音结束一句，连续长句最长约 8 秒。
- 建立音频分段队列，逐句进行语音转写和翻译。

## v3.5.1

- 扩展视频查找范围到开放 Shadow DOM、同源 iframe 和已发现根节点。
- 在顶层页面与内嵌课程播放器之间广播字幕启动和停止状态。
- 改善封装播放器的发现和控制。

## v3.5.0

- 首次增加视频中文字幕模块。
- 支持读取网页已有英文字幕轨并翻译。
- 对无字幕视频尝试捕获音轨，接入 SiliconFlow SenseVoice 等在线转写接口。
- 在页面顶层显示字幕覆盖层。

## v3.4.2

- 将右下角控制条放入浏览器顶层 Popover。
- 周期性检查并恢复被站点重绘移除或遮挡的控制条。
- 固定最高层级、位置、可见性和布局样式。

## v3.4.1

- 控制条只在顶层页面挂载，避免被内嵌画布或 iframe 包住。
- 改为挂载到文档根节点，提高复杂页面中的稳定性。

## v3.4.0

- 区分普通输入控件和只读富文本，避免误跳过项目正文。
- 增强控制条的固定定位和最高层级样式。
- 修复控制条被 Higgsfield 页面样式覆盖的问题。

## v3.3.3

- 扩展 About the project 富文本容器匹配规则。
- 支持 `.rde-root`、Lexical、ARIA 多行文本和更多 `contenteditable` 结构。

## v3.3.2

- 修复仅中文模式下复杂富文本消失的问题。
- 为代码块和富文本保存原始 HTML，并在模式切换时可靠恢复。
- 仅中文与中英对照切换时保持原始结构。

## v3.3.1

- 加强富文本段落识别，覆盖标题、正文、列表、引用和叶子级容器。
- 修复 About the project 中部分段落没有进入翻译队列的问题。

## v3.3.0

- 默认启用第三方 AI 翻译。
- 专门识别 Higgsfield 的富文本编辑器与 About the project 容器。
- 排除网址和电子邮件，减少无意义翻译请求。

## v3.2.0

- 新增“全局强制翻译”和“当前阅读区域”两种扫描范围。
- 增加长文本自动拆分、不完整批次缩小补译和本地译文缓存。
- SiliconFlow 默认关闭推理模式，降低 Token 消耗。
- 术语表只选择当前文字命中的条目发送。
- 兼容模型的 `reasoning_content`、数组消息和 `<think>` 内容清理。

## v3.0.0

- 产品从翻译助手升级为 AI 影视知识助手。
- 新增 Community Project 快速提炼、工作流还原、TapNow 迁移和项目问答。
- 新增最多 100 张本地知识卡及 Markdown、JSON 导出。
- 扩展到 OpenAI Responses、Anthropic Messages、Gemini 和 Ollama 等五类协议。
- 增加 OpenRouter、Qwen、SiliconFlow、Moonshot、Groq、Mistral 等服务预设。

## v2.0.0

- 从纯本地翻译升级为中英对照与第三方 AI 翻译。
- 新增仅中文、仅原文显示模式。
- 新增 DeepSeek、OpenAI 和 OpenAI 兼容接口。
- 首次加入可导入、导出的专有名词库。
- 对提示词代码块进行整块翻译并保留高亮、控制标签和 `@` 资产引用。

## v1.0.0

- 首个可用版本。
- 使用 Chrome 本地翻译把 Higgsfield 英文转换为简体中文。
- 动态监听项目介绍、弹窗和展开内容。
- 覆盖标题、按钮、属性文字、开放 Shadow DOM 和匹配域名的内嵌页面。
- 提供网页右下角中文/原文切换和重新扫描。

